#include <iostream>
#include <climits>
#include <cstring>
using namespace std;
const int N = 1e5 + 5;
int n;
int a[N];
//f(x)=


int main() {
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	f(0);
	for (int i = 0; i < n; i++) {
		cout << a[i];
	}
	return 0;
}